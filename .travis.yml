language: python

python:
  - "2.7"
  
services:
  - docker

branches:
    only:
        - master
  
notifications:
  email: false
  slack: aices-pcm-cfd:ShncJAFCJzx48zt8hrXpRwGw

before_install:
- docker pull zimmerman/fenics-pytest:latest

script:
- docker run -P -v `pwd`:/home/fenics/phaseflow:rw zimmerman/fenics-pytest:latest /bin/sh -c "cd phaseflow; pytest"
