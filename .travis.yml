language: python

python:
  - "2.7"
  
services:
  - docker

branches:
    only:
        - master
  
notifications:
  email: false

before_install:
- docker pull zimmerman/phaseflow-fenics:latest

group: deprecated-2017Q2

script:
- docker run --rm -P -v `pwd`:/home/fenics/shared -w /home/fenics/shared zimmerman/phaseflow-fenics:latest "pip install --user --upgrade . && python -m pytest -v -k '(not nightly)'" 
